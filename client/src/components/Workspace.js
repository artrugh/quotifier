import React from "react";
import LogOutButton from "./LogOutButton";

const Workspace = () => {
  return <div className="workspace">You are logged in. {LogOutButton}</div>;
};

export default Workspace;
